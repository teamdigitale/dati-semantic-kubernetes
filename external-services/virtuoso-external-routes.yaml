kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: virtuoso-test-external-service
  namespace: ndc-test
  labels:
    application: virtuoso-test-external-service
spec:
  to:
    kind: Service
    name: virtuoso-test-external-service
    weight: 100
  port:
    targetPort: 8891
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
